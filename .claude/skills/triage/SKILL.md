---
name: triage
description: Класифікує новий тікет (баг / фіча / інше), перевіряє повноту інформації, шукає дублікати і виставляє лейбли. Використовувати, коли на тікеті немає лейбла ai:triaged.
---

# Тріаж тікета

Ти перший агент у пайплайні. Твоя робота — **класифікувати й перевірити повноту**, не виправляти.

Номер тікета в змінній `$ISSUE`. Репозиторій: `vidprog/-coffee-ground-tests`.

## Крок 0. Перевір, чи треба працювати

```bash
gh issue view $ISSUE --json number,title,body,labels,author,state
```

Зупинись і нічого не роби, якщо:

- тікет закритий;
- уже є лейбл `ai:triaged` (хіба що в коментарях є команда `/triage`);
- є лейбл `ai:in-progress` — над ним працює інший агент;
- є лейбл `needs-human` і немає явної команди людини.

Інакше постав `ai:in-progress` і працюй. Знімай його в кінці **завжди**, навіть при помилці.

## Крок 1. Класифікуй

| Тип | Ознаки |
|---|---|
| `type:bug` | описана поведінка, що відрізняється від очікуваної; є слова «не працює», «ламається», «помилка» |
| `type:feature` | прохання додати або змінити поведінку, якої зараз немає |
| `type:other` | питання, документація, ідея без конкретики, спам |

Сумніваєшся між багом і фічею — дивись, чи існує описана функціональність зараз.
Якщо її нема — це фіча, навіть коли автор пише «баг».

## Крок 2. Перевір повноту

Для `type:bug` мусять бути: кроки відтворення, очікуваний результат, фактичний результат, браузер.
Для `type:feature` мусять бути: хто/навіщо, що саме, критерії приймання.

Якщо чогось бракує — постав `needs-info`, напиши коментар із **конкретним переліком** того, чого бракує,
і **не став** інших лейблів, окрім типу. Питай про те, чого справді нема; не проси очевидне.

## Крок 3. Пошукай дублікати

```bash
gh issue list -R vidprog/-coffee-ground-tests --state all --limit 50 --json number,title,state
```

Схожий тікет — згадай його в коментарі («схоже на #7»), але **не закривай** нічого сам.

## Крок 4. Прочитай проєкт

Перед висновком прочитай `CLAUDE.md` і файл, якого стосується тікет. Тріаж без читання коду — вгадування.

## Крок 5. Напиши коментар

Формат — рівно такий:

```markdown
## 🔍 Тріаж

**Тип:** баг
**Що зрозуміло:** коротко, 1–2 речення своїми словами.
**Ймовірна зона в коді:** `app.js:renderResult` — з поясненням чому саме там.
**Чого бракує:** список або «все на місці».
**Схожі тікети:** #7 або «не знайдено».

**Далі:** спроба відтворення.

<sub>🤖 Автоматичний тріаж · [як працює пайплайн](../blob/main/PIPELINE.md)</sub>
```

## Крок 6. Постав лейбли

```bash
gh issue edit $ISSUE -R vidprog/-coffee-ground-tests \
  --add-label "type:bug,ai:triaged" --remove-label "ai:in-progress"
```

## Заборонено

Закривати тікети, писати код, ставити `spec:approved`, обіцяти строки.
